# Tuple in Python: Check Element Existence

## 🎯 Aim
To write a Python program that checks if the element `'n'` and the element `8` exist within a given tuple.

## 🧠 Algorithm
1. Define a tuple `x` with some letters and numbers.
2. Use the `in` operator to check if the string `'n'` exists within the tuple.
3. Use the `in` operator to check if the integer `8` exists within the tuple.
4. Print the results.

## 🧾 Program
Define a tuple
x = ('a', 'b', 'n', 5, 8, 10)

Check if 'n' exists
if 'n' in x: print("'n' exists in the tuple") else: print("'n' does not exist in the tuple")

Check if 8 exists
if 8 in x: print("8 exists in the tuple") else: print("8 does not exist in the tuple")

## Output

<img width="544" height="240" alt="image" src="https://github.com/user-attachments/assets/a5863534-6f60-407f-95f0-ed02b6cc2cd3" />

## Result
Thus, the Python program successfully checks whether the elements 'n' and 8 exist in the given tuple using the in operator.
